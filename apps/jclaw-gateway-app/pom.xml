<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.jclaw</groupId>
        <artifactId>jclaw-apps</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>jclaw-gateway-app</artifactId>
    <name>JClaw :: Gateway App</name>
    <description>Standalone Spring Boot application for the JClaw gateway (REST + WebSocket + channels)</description>

    <dependencies>
        <!-- Gateway starter (auto-config + gateway library + all channel adapters + web) -->
        <dependency>
            <groupId>io.jclaw</groupId>
            <artifactId>jclaw-starter-gateway</artifactId>
            <type>pom</type>
        </dependency>

        <!-- Code tools (file_edit, glob, grep) -->
        <dependency>
            <groupId>io.jclaw</groupId>
            <artifactId>jclaw-code</artifactId>
        </dependency>

        <!-- All four AI providers -->
        <dependency>
            <groupId>io.jclaw</groupId>
            <artifactId>jclaw-starter-anthropic</artifactId>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>io.jclaw</groupId>
            <artifactId>jclaw-starter-openai</artifactId>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>io.jclaw</groupId>
            <artifactId>jclaw-starter-ollama</artifactId>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>io.jclaw</groupId>
            <artifactId>jclaw-starter-gemini</artifactId>
            <type>pom</type>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>jbang</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <configuration>
                            <!-- Fat JAR becomes *-exec.jar, original JAR stays as the
                                 standard Maven artifact so JBang can resolve it. -->
                            <classifier>exec</classifier>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>k8s</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.eclipse.jkube</groupId>
                        <artifactId>kubernetes-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
